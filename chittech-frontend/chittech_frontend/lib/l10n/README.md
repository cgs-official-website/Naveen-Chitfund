# l10n (stub)

Regional-language UI is explicitly out of scope for this MVP. This folder is a
placeholder so adding Flutter's `gen_l10n` / ARB-based localization later
doesn't require restructuring the app — strings are currently inline in
widgets, but are short and centralized enough per-screen to extract when this
is prioritized.
